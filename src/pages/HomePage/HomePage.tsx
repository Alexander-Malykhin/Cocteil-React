import {type FC} from 'react';
//components
import {Header} from "@widgets/Header";

const HomePage: FC = () => {
    return (
        <>
            <Header/>
        </>
    );
};

export default HomePage;